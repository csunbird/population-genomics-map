# R
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# renv — keep lockfile and activate script, ignore the actual library
renv/library/
renv/staging/
renv/sandbox/
.Rprofile  # regenerated by renv::activate()

# Shiny / rsconnect deployment
rsconnect/

# Large data files — don't commit real genomic data to the repo
data/user/
*.vcf
*.vcf.gz
*.bam
*.bai
*.bcf

# Keep demo data (small, pre-committed CSV only)
!data/demo/demo_samples.csv
!data/demo/generate_demo.R

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
.vscode/
